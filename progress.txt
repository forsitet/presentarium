## TASK-006: Frontend auth pages — DONE (2025-11-14)

### What was done:
- Created `frontend/src/components/AuthLayout.tsx` — centered card layout for auth pages with gradient background
- Implemented full `frontend/src/pages/LoginPage.tsx`:
  - react-hook-form + zod schema validation (email format, password ≥8 chars)
  - Calls POST /api/auth/login, saves token+user to authStore, redirects to /dashboard
  - Handles 401 (wrong credentials), 429 (rate limit), generic errors
- Implemented full `frontend/src/pages/RegisterPage.tsx`:
  - react-hook-form + zod (name, email, password ≥8 chars)
  - Calls POST /api/auth/register, on success logs in and redirects to /dashboard
  - Handles 409 Conflict (email taken), 400 validation errors
- `src/api/client.ts` was already complete (axios + Bearer interceptor + auto-refresh on 401)
- `authStore.ts` was already complete (zustand persist — user, accessToken, isAuthenticated)
- `routes/index.tsx` was already complete (ProtectedRoute → redirect to /login)

### Files changed:
- frontend/src/components/AuthLayout.tsx (new)
- frontend/src/pages/LoginPage.tsx (implemented)
- frontend/src/pages/RegisterPage.tsx (implemented)
- tasks.json (TASK-006 status → done)

---

## TASK-009: Dashboard with polls list — DONE (2026-03-09)

### What was done:
- Created `frontend/src/api/polls.ts` — getPolls, deletePoll, copyPoll, createRoom API functions
- Created `frontend/src/components/PollCard.tsx` — card with title, description, date, Edit/Launch/Copy/Delete buttons
- Created `frontend/src/components/ConfirmDialog.tsx` — reusable modal confirmation dialog
- Implemented full `frontend/src/pages/DashboardPage.tsx`:
  - Polls grid with responsive layout (1/2/3 columns)
  - Loading skeletons (animate-pulse) while fetching
  - Empty state with "Create poll" CTA
  - Delete confirmation via ConfirmDialog
  - Copy creates new card at top of list
  - Launch calls POST /api/rooms, navigates to /host/:code
  - Error banner with dismiss
  - Header with username and logout
- Updated `frontend/src/routes/index.tsx` with routes: /polls/new, /polls/:id/edit, /host/:code, /join, /join/:code
- Added stub pages: PollEditorPage.tsx, HostSessionPage.tsx, JoinPage.tsx

### Files changed:
- frontend/src/api/polls.ts (new)
- frontend/src/components/PollCard.tsx (new)
- frontend/src/components/ConfirmDialog.tsx (new)
- frontend/src/pages/DashboardPage.tsx (implemented)
- frontend/src/pages/PollEditorPage.tsx (new stub)
- frontend/src/pages/HostSessionPage.tsx (new stub)
- frontend/src/pages/JoinPage.tsx (new stub)
- frontend/src/routes/index.tsx (added new routes)
- tasks.json (TASK-009 status → done)

---

## TASK-010: Poll editor — DONE (2026-03-10)

### What was done:
- Extended `frontend/src/api/polls.ts` with 8 new functions: getPoll, createPoll, updatePoll, getQuestions, createQuestion, updateQuestion, deleteQuestion, reorderQuestions
- Created `frontend/src/components/QuestionTypeSelector.tsx`: modal overlay with 2x3 grid of question type cards (all 6 types), Russian labels, Escape key support
- Created `frontend/src/components/QuestionEditorPanel.tsx`:
  - Common fields: text (textarea, max 500 chars), time_limit_seconds (5-300), points
  - choice types (single/multiple/image): colored option bars, add/remove options, correct answer toggle (radio for single, checkbox for multiple), min 2 / max 6 options
  - Non-choice types: informational message
  - Auto-save on blur with "Сохранено" flash confirmation
- Created `frontend/src/components/QuestionPreview.tsx`: dark-themed participant view, timer+points badges, colored option buttons for choice types, placeholder text for open types
- Implemented full `frontend/src/pages/PollEditorPage.tsx`:
  - Two-column desktop layout (question list + editor/preview), single-column mobile
  - Poll settings form: title, description, scoring_rule, question_order — 500ms debounce auto-save
  - Create-on-new flow: poll created when first save or first question added
  - URL updates via window.history.replaceState after creation
  - Question list with numbered cards, type badges, ↑/↓ reorder buttons
  - Delete via ConfirmDialog
  - Loading skeleton, error banner, empty states
  - Save status indicator in sticky header

### Files changed:
- frontend/src/api/polls.ts (extended)
- frontend/src/components/QuestionTypeSelector.tsx (new)
- frontend/src/components/QuestionEditorPanel.tsx (new)
- frontend/src/components/QuestionPreview.tsx (new)
- frontend/src/pages/PollEditorPage.tsx (full implementation)
- tasks.json (TASK-010 status → done)

### Notes for next iteration:
- Next priority: TASK-015 (high) — Host waiting screen (depends on TASK-006 ✓, TASK-014 ✓)
- TASK-015 needs: HostSessionPage in 'waiting' state, QR code, participant list via WebSocket, "Start" button
- TASK-016 (JoinPage) depends on TASK-014 ✓ and TASK-015, also ready after TASK-015
- TASK-035 (Backend middleware) is also high priority and fully ready (depends on TASK-005 ✓ only)

---

## TASK-015: Host waiting screen — DONE (2026-03-10)

### What was done:
- Added API functions to `frontend/src/api/polls.ts`: `getRoomParticipants`, `changeRoomState`, `getRoomInfo`
- Created `frontend/src/stores/sessionStore.ts`: Zustand store managing roomCode, pollId, roomStatus, participants, currentQuestion, leaderboard, answerCount with full CRUD actions
- Created `frontend/src/components/ParticipantList.tsx`: shows participant avatars with initials, empty-state prompt, participant count badge
- Implemented full `frontend/src/pages/HostSessionPage.tsx` (waiting state):
  - Connects to WebSocket as organizer (JWT token passed to socket.connect)
  - Loads initial participants via GET /api/rooms/:code/participants
  - Listens for `participant_joined` / `participant_left` WS events
  - Displays 6-digit room code in large monospace font
  - QR code via `qrcode.react` (QRCodeSVG) pointing to `/join/:code`
  - Copy-link button with clipboard API and "✓ Скопировано!" feedback
  - "Начать опрос (N)" button — disabled when 0 participants, calls PATCH /api/rooms/:code/state { action: 'start' }
  - Cleans up WS handlers and disconnects on unmount

### Files changed:
- frontend/src/api/polls.ts (extended with 3 new functions)
- frontend/src/stores/sessionStore.ts (new)
- frontend/src/components/ParticipantList.tsx (new)
- frontend/src/pages/HostSessionPage.tsx (full implementation)
- tasks.json (TASK-015 status → done)

### Notes for next iteration:
- Next: TASK-016 (high) — JoinPage for participants (depends on TASK-014 ✓, TASK-015 ✓)
- TASK-016 needs: code input, name input, WS connect as participant, session_token in localStorage, auto-reconnect, waiting screen after join
- TASK-020 (HostSessionPage quiz state) and TASK-021 (participant quiz screen) become unblocked once TASK-016 is done

---

## TASK-011: Backend image upload — DONE (2025-11-20)

### What was done:
- Created `backend/internal/handler/upload_handler.go`:
  - POST /api/upload/image: accepts multipart/form-data with field name "image"
  - Enforces 5 MB limit via http.MaxBytesReader + explicit byte count check
  - Detects MIME type from magic bytes (NOT from Content-Type header): JPEG (FF D8 FF), PNG (89 50 4E 47...), WebP (RIFF...WEBP)
  - Rejects unsupported types with 415 Unsupported Media Type
  - Saves file as {uuid}.{ext} in {uploadsDir}/images/ (creates dir if missing)
  - Returns JSON: {"image_url": "/uploads/images/uuid.ext"}
- Updated `backend/internal/handler/routes.go`:
  - Added `UploadsDir string` field to `RouterDeps`
  - Wired `uploadH.handleImage` to POST /api/upload/image (inside JWT-protected group)
  - Added `/uploads/*` static file server at router level (public, no JWT needed for viewing)
- Updated `backend/cmd/server/main.go`: passes `cfg.UploadsDir` to RouterDeps

### Files changed:
- backend/internal/handler/upload_handler.go (new)
- backend/internal/handler/routes.go (UploadsDir field, upload route, static serving)
- backend/cmd/server/main.go (pass UploadsDir)
- tasks.json (TASK-011 status → done)

### Notes for next iteration:
- TASK-016 (high): JoinPage with auto-reconnect — all deps done
- TASK-020 (high): HostSessionPage quiz state — all deps done
- TASK-035 (high): Backend CORS/logging/rate-limit middleware — all deps done

---

## TASK-016: Frontend JoinPage + ParticipantSessionPage — DONE (2026-02-18)

### What was done:
- Fixed `frontend/src/ws/socket.ts`: reset `maxReconnects = 5` in `connect()` to ensure reconnect works after a previous `disconnect()` call
- Implemented full `frontend/src/pages/JoinPage.tsx`:
  - react-hook-form + zod validation (6-digit code, name 2-30 chars)
  - Connects WebSocket as participant (no JWT — only name in query param)
  - Waits for `connected` WS message → navigates to `/session/:code`
  - 5-second timeout fallback → shows error + disconnects
  - Code input pre-filled from URL param `/join/:code`
  - Spinner button during connection attempt
- Created `frontend/src/pages/ParticipantSessionPage.tsx`:
  - Redirects to `/join/:code` if no `session_token` in localStorage (handles page refresh without prior join)
  - Calls `socket.connect()` on mount to restore connection after page refresh (session_token auto-sent from localStorage)
  - Waiting state: dual-ring spinner animation + bouncing dots + "Ждём начала..." copy
  - Listens for `room_state_changed` and `session_end` WS events
  - Finished state: "Опрос завершён!" screen with "На главную" button
- Updated `frontend/src/routes/index.tsx`: added `/session/:code` → ParticipantSessionPage (public route)

### Files changed:
- frontend/src/ws/socket.ts (bug fix: reset maxReconnects in connect())
- frontend/src/pages/JoinPage.tsx (full implementation)
- frontend/src/pages/ParticipantSessionPage.tsx (new — waiting + finished states)
- frontend/src/routes/index.tsx (added /session/:code route)
- tasks.json (TASK-016 status → done)

### Notes for next iteration:
- Next: TASK-020 (high) — HostSessionPage quiz state (showing_question, results, leaderboard) — all deps done
- TASK-021 (high) — ParticipantSessionPage question/answer UI — depends on TASK-016 ✓ and TASK-019 ✓
- TASK-035 (high) — Backend CORS/logging/rate-limit middleware — depends on TASK-005 ✓

---

## TASK-020: HostSessionPage quiz states — DONE (2026-03-11)

### What was done:
- Created `frontend/src/components/AnswerBarChart.tsx`:
  - Uses recharts (BarChart, Bar, Cell, XAxis, YAxis, Tooltip, ResponsiveContainer)
  - Props: options[], distribution (Record<string,number>), showCorrect flag
  - Colors: neutral palette during question, green for correct / red for incorrect in results
  - Truncates long option labels to 22 chars
- Created `frontend/src/components/Leaderboard.tsx`:
  - Shows top-N entries with rank, name, score
  - Gold/silver/bronze colors for ranks 1/2/3
  - Empty state handled gracefully
- Implemented full `frontend/src/pages/HostSessionPage.tsx` quiz states:
  - **active**: transition screen after start — shows first question preview, "Show first question" button
  - **showing_question**: live question view — timer progress bar (green→yellow→red), answer counter, Kahoot-style option tiles, real-time AnswerBarChart, "End early" button
  - **showing_results**: post-question view — AnswerBarChart with correct answers highlighted (green/red), option list with ✓/✗ markers, Leaderboard top-5, next question preview with "Show next" button
  - **finished**: final screen with trophy, final Leaderboard, "Back to dashboard" button
  - WS handlers: question_start, timer_tick, answer_count, question_end, results, leaderboard, session_end
- Fixed `frontend/src/pages/DashboardPage.tsx`: passes `{ state: { pollId } }` in navigate() when launching a room so HostSessionPage can load questions

### Files changed:
- frontend/src/components/AnswerBarChart.tsx (new)
- frontend/src/components/Leaderboard.tsx (new)
- frontend/src/pages/HostSessionPage.tsx (full implementation — all quiz states)
- frontend/src/pages/DashboardPage.tsx (pass pollId in navigation state)
- tasks.json (TASK-020 status → done)

### Notes for next iteration:
- Next: TASK-021 (high) — ParticipantSessionPage question/answer screen — all deps done (TASK-016 ✓, TASK-019 ✓)
- TASK-021 needs: question_start → show question+options+timer; answer submission; answer_accepted state; question_end → show correct/wrong + score + rank
- TASK-035 (high) — Backend CORS/logging/rate-limit middleware — depends on TASK-005 ✓

---

## TASK-021: ParticipantSessionPage — question/answer UI — DONE (2026-03-11)

### What was done:
- Fully implemented `frontend/src/pages/ParticipantSessionPage.tsx` with all quiz states:
  - **waiting/active**: animated spinner screen (unchanged)
  - **showing_question**: question text, timer progress bar (green→yellow→red), answer inputs per type
  - **answer submitted**: "Ответ принят!" overlay, buttons blocked
  - **showing_results**: correct/wrong option highlighting (green/red), earned score badge, rank display
  - **finished**: trophy screen with podium top-3, full ranked list, final score

### Components implemented (inline):
- `SingleChoiceInput`: 2-6 Kahoot-style colored buttons (▲◆●■★♥), one tap = submitted
- `MultipleChoiceInput`: checkboxes grid + "Подтвердить (N)" button
- `TextInput`: textarea with char counter + send button, supports open_text and word_cloud (max 50 chars)
- `TimerBar`: animated progress bar with color transition
- `ResultsScreen`: per-option correct/wrong coloring (green=correct, red=selected-wrong), rank + score
- `FinalScreen`: trophy emoji, my_rank + my_score, podium (🥇🥈🥉), ranked list top-10

### WS events handled:
- `question_start` → reset all state, show question
- `timer_tick` → update countdown
- `answer_accepted` → record score/is_correct
- `question_end` → reveal options with is_correct flags, switch to results view
- `leaderboard` → update my_rank + my_score
- `session_end` → final rankings + final screen

### Files changed:
- frontend/src/pages/ParticipantSessionPage.tsx (full rewrite)
- tasks.json (TASK-021 status → done)

### Notes for next iteration:
- Next high priorities: TASK-022 (word cloud backend), TASK-035 (CORS/logging middleware), TASK-024 (brainstorm backend)
- TASK-034 (participant finish screens) is now unblocked — but FinalScreen is partially covered in TASK-021 already
- TASK-033 (real-time stats for organizer) is also unblocked

---

## TASK-035: Backend middleware — CORS, logging, rate limiting — DONE (2026-03-11)

### What was done:
- Created `backend/internal/middleware/cors.go`:
  - `CORS(allowedOrigin string)` middleware: sets Access-Control-Allow-Origin only for matching origin
  - Handles preflight OPTIONS with 204 + required CORS headers
  - Sets `Vary: Origin` header to avoid caching issues
- Created `backend/internal/middleware/logging.go`:
  - `Logger()` middleware: wraps ResponseWriter to capture status code
  - Logs every request via `slog.Info`: method, path, status, duration_ms
- Updated `backend/internal/handler/routes.go`:
  - Added `CORSAllowedOrigin string` field to `RouterDeps`
  - Wired `CORS` and `Logger` at the top of the middleware chain (before RequestID/RealIP/Recoverer)
- Updated `backend/cmd/server/main.go`: passes `cfg.CORSAllowedOrigin` to RouterDeps

### Existing middleware already in place (pre-existing):
- Auth middleware: JWT extraction from Authorization header → user_id in context
- Rate limit middleware: 5 req/min per IP on /api/auth/* routes

### Files changed:
- backend/internal/middleware/cors.go (new)
- backend/internal/middleware/logging.go (new)
- backend/internal/handler/routes.go (CORSAllowedOrigin field, CORS+Logger wiring)
- backend/cmd/server/main.go (pass CORSAllowedOrigin)
- tasks.json (TASK-035 status → done)

### Notes for next iteration:
- Next high priorities: TASK-022 (word cloud backend, all deps done), TASK-024 (brainstorm backend, all deps done)
- TASK-036 (Nginx reverse proxy) — all deps done (TASK-002 ✓, TASK-004 ✓)
- TASK-039 (unit tests) depends on TASK-022 and TASK-026

---

## TASK-022: Backend word cloud — normalization, aggregation, wordcloud_update — DONE (2025-11-25)

### What was done:
- `pkg/badwords/filter.go`: added `LoadFromFile(path string) error` — reads JSON array from disk and loads into dictionary
- `pkg/badwords/badwords.json`: initial dictionary with 26 RU+EN profanity words
- `pkg/normalize/text_test.go`: unit tests for `normalize.Text()` — trim, lowercase, punctuation removal, multi-case equality
- `pkg/badwords/filter_test.go`: unit tests for `Filter()` — clean text, single bad word, english, case-insensitive, empty input
- `internal/ws/room.go`:
  - Added `Type string` field to `ActiveQuestion` (so question type is available without a DB call during answer processing)
  - Added `wordCloudFreq map[string]int` to `Room` struct
  - `ResetAnswerCount()` now also resets the word cloud freq map
  - New methods: `AddWordCloudWords(words []string)`, `WordCloudTopWords(n int) []WordcloudWord` (top-N by freq, insertion sort)
- `internal/service/conduct_service.go`:
  - `handleShowQuestion()`: sets `activeQ.Type = q.Type`
  - `handleSubmitText()`: if question type is `word_cloud`, filters text through `badwords.Filter()`, saves filtered text to DB, then normalizes and splits into words, updates room freq map, broadcasts `wordcloud_update` to organizer
  - Added imports: `strings`, `presentarium/pkg/badwords`, `presentarium/pkg/normalize`
- `cmd/server/main.go`: loads `pkg/badwords/badwords.json` on startup via `badwords.LoadFromFile()`

### Files changed:
- backend/pkg/badwords/filter.go (LoadFromFile added)
- backend/pkg/badwords/badwords.json (new)
- backend/pkg/normalize/text_test.go (new)
- backend/pkg/badwords/filter_test.go (new)
- backend/internal/ws/room.go (Type in ActiveQuestion, wordCloudFreq, AddWordCloudWords, WordCloudTopWords)
- backend/internal/service/conduct_service.go (word cloud logic in handleSubmitText)
- backend/cmd/server/main.go (load badwords on startup)
- tasks.json (TASK-022 status → done)

### Notes for next iteration:
- TASK-023 (frontend word cloud) is now unblocked — needs WordCloudView.tsx with react-wordcloud, WordCloudInput.tsx
- TASK-024 (brainstorm backend) is high priority, all deps done (TASK-018 ✓)
- TASK-026 (badwords filter — full TASK-026 scope) — partial work done here, remaining: hot reload, TASK-026 moderation endpoint
- TASK-036 (Nginx) — all deps done
- TASK-039 (unit tests) — TASK-022 ✓ now, still needs TASK-026

---

## TASK-024: Backend brainstorm mode — DONE (2026-01-10)

### What was done:
- Created `backend/internal/repository/brainstorm_repo.go`:
  - `BrainstormRepository` interface with 8 methods
  - `CreateIdea`, `GetIdea`, `CountIdeasByParticipant`, `SetIdeaHidden`, `ListIdeasRanked`, `ListAllIdeas`
  - `CreateVote` (returns ErrConflict on UNIQUE violation via pq.Error code 23505), `CountVotesByParticipant`
  - DB trigger on `brainstorm_votes` keeps `brainstorm_ideas.votes_count` in sync
- Extended `backend/internal/ws/room.go`:
  - Added `brainstormPhase`, `brainstormIdeaCounts`, `brainstormVoteCounts` fields to Room struct
  - Added `InitBrainstorm()`, `BrainstormPhase()`, `SetBrainstormPhase()`, `IncrementBrainstormIdeaCount()`, `BrainstormIdeaCount()`, `IncrementBrainstormVoteCount()`, `BrainstormVoteCount()` methods
- Extended `backend/internal/service/conduct_service.go`:
  - Added `brainstormRepo` field and updated `NewConductService` signature
  - `handleShowQuestion`: calls `room.InitBrainstorm()` for brainstorm question type
  - `HandleMessage`: routes `submit_idea`, `submit_vote`, `brainstorm_hide_idea`, `brainstorm_change_phase`
  - `handleSubmitIdea`: validates collecting phase, 5-idea limit (in-memory), badwords filter, saves to DB, broadcasts `brainstorm_idea_added` to organizer + submitter
  - `handleSubmitVote`: validates voting phase, checks own-idea prohibition, 3-vote limit (in-memory), UNIQUE check via ErrConflict, broadcasts `brainstorm_vote_updated` with updated `votes_count`
  - `handleBrainstormHideIdea`: organizer toggles `is_hidden`, broadcasts `answer_hidden` to all
  - `handleBrainstormChangePhase`: organizer advances phase (collecting→voting→results); on results, fetches ranked ideas from DB and broadcasts them with phase change
- Updated `backend/cmd/server/main.go`: creates `brainstormRepo` and passes it to `NewConductService`

### Files changed:
- backend/internal/repository/brainstorm_repo.go (new)
- backend/internal/ws/room.go (brainstorm state + 8 new methods)
- backend/internal/service/conduct_service.go (brainstormRepo, 4 new handlers)
- backend/cmd/server/main.go (wire brainstorm repo)
- tasks.json (TASK-024 status → done)

### Notes for next iteration:
- TASK-025 (frontend brainstorm UI) is now unblocked — needs BrainstormInput.tsx, BrainstormBoard.tsx
- TASK-023 (frontend word cloud) is also high priority — needs WordCloudView.tsx with react-wordcloud
- TASK-026 ✓ DONE — hot reload, bypass detection, open_text filter all implemented
- TASK-036 (Nginx reverse proxy) — high priority, all deps done

---

## TASK-026: Backend badwords filter — bypass detection, open_text, hot reload — DONE (2025-12-05)

### What was done:
- `pkg/badwords/filter.go`:
  - Added bypass detection: `Filter()` strips all spaces from input, normalizes, and checks if the result is a single bad word (handles 'н е ц е н з у р н о е' style evasion)
  - Added `WatchFile(path string, interval time.Duration)`: background goroutine polls file mtime every `interval`; on change calls `LoadFromFile` — hot reload without server restart
- `pkg/badwords/filter_test.go`: added `TestFilter_SpacesBypass` and `TestFilter_SpacesBypassRussian` tests
- `internal/service/conduct_service.go`: `handleSubmitText` now applies badwords filter to **both** `open_text` and `word_cloud` (previously only `word_cloud`)
- `cmd/server/main.go`: after loading dictionary, calls `badwords.WatchFile(path, 10*time.Second)`; added `"time"` import

### Files changed:
- backend/pkg/badwords/filter.go (WatchFile, bypass detection)
- backend/pkg/badwords/filter_test.go (bypass tests)
- backend/internal/service/conduct_service.go (open_text filter)
- backend/cmd/server/main.go (wire WatchFile)
- tasks.json (TASK-026 status → done)

### Notes for next iteration:
- TASK-027 (manual moderation) — now unblocked (depends on TASK-022 ✓, TASK-024 ✓, TASK-026 ✓)
- TASK-036 (Nginx) — high priority, all deps done
- TASK-039 ✓ DONE — unit tests for scoring, room state machine, word cloud, brainstorm

---

## TASK-039: Backend unit tests — scoring, room state machine, word cloud — DONE (2026-01-15)

### What was done:
- Created `backend/pkg/scoring/scoring_test.go`:
  - `TestCalculateScore_NoneRule`: none rule always returns 0 (correct, wrong, nil isCorrect)
  - `TestCalculateScore_CorrectAnswerRule`: full basePoints for correct, 0 for wrong/nil
  - `TestCalculateScore_SpeedBonus_Maximum`: responseTimeMs=0 → full basePoints
  - `TestCalculateScore_SpeedBonus_Minimum`: responseTimeMs=timeLimitMs → minimum 10%
  - `TestCalculateScore_SpeedBonus_HalfTime`: halfway through → ~50% score
  - `TestCalculateScore_SpeedBonus_WrongAnswer`: speed_bonus + wrong → 0
  - `TestCalculateScore_SpeedBonus_NilIsCorrect`: speed_bonus + nil → 0
  - `TestCalculateScore_SpeedBonus_MinimumFloor`: over-time → 10% floor
  - `TestCalculateScore_SpeedBonus_ZeroTimeLimit`: zero time limit → basePoints (no div-by-zero)
  - `TestCalculateScore_UnknownRule`: unknown rule → 0
  - `TestCalculateScore_SpeedBonus_MinimumFloor_SmallBase`: small base → at least 1 point
- Created `backend/internal/ws/room_test.go` (white-box, package ws):
  - State machine: initial state, SetState through all states
  - TryFinishQuestion: success, wrong ID, no active question, double-finish prevention
  - Answer count: IncrementAnswerCount, ResetAnswerCount
  - Word cloud: AddWordCloudWords, WordCloudTopWords ordering, top-N limit, empty state, reset clears cloud
  - Brainstorm: InitBrainstorm, SetBrainstormPhase, per-participant idea count, per-participant vote count
  - Client tracking: ParticipantCount, ClientCount, RemoveClient, Organizer tracking
  - Concurrency: concurrent IncrementAnswerCount (50 goroutines), concurrent AddWordCloudWords (20 goroutines) — race-safe

### Files changed:
- backend/pkg/scoring/scoring_test.go (new — 11 test functions)
- backend/internal/ws/room_test.go (new — 20+ test functions including concurrency tests)
- tasks.json (TASK-039 status → done)

### Notes for next iteration:
- TASK-036 ✓ DONE
- TASK-040 (integration tests) — now unblocked (depends on TASK-039 ✓)
- TASK-027 (manual moderation backend) — medium priority, all deps done
- TASK-023 (frontend word cloud) — medium, all deps done
- TASK-025 (frontend brainstorm UI) — medium, all deps done

---

## TASK-029: Frontend session history page and results detail — DONE (2025-12-02)

### What was done:
- Extended `frontend/src/types/index.ts` with `SessionSummary`, `QuestionStat`, `SessionDetail` types
- Added `getSessions()` and `getSession()` to `frontend/src/api/polls.ts`
- Created `frontend/src/pages/PollResultsPage.tsx`:
  - Session header: poll name, date range, 3-stat grid (participants, avg score, question count)
  - Export buttons: «Скачать CSV» (direct `/api/sessions/:id/export/csv` link), «Скачать PDF» (disabled placeholder for TASK-031)
  - Final leaderboard using existing `Leaderboard` component (dark bg)
  - Per-question stats: choice types → `AnswerBarChart` (with correct answer highlighting, options fetched from poll), open_text/word_cloud → `TextResponses` inline list, brainstorm → total count
- Updated `frontend/src/pages/DashboardPage.tsx`:
  - Added `SessionHistoryTab` component: loading skeleton, empty state, session rows with poll title / date / participant count / avg score, clickable → navigates to `/sessions/:id`
  - Added tab switcher (Мои опросы | История) in page header
- Updated `frontend/src/routes/index.tsx`: added `/sessions/:id` protected route → PollResultsPage

### Files changed:
- frontend/src/types/index.ts (SessionSummary, QuestionStat, SessionDetail types)
- frontend/src/api/polls.ts (getSessions, getSession)
- frontend/src/pages/PollResultsPage.tsx (new)
- frontend/src/pages/DashboardPage.tsx (SessionHistoryTab + tab switcher)
- frontend/src/routes/index.tsx (/sessions/:id route)
- tasks.json (TASK-029 status → done)

### Notes for next iteration:
- TASK-030 (CSV export backend) — now unblocked (deps: TASK-028 ✓); CSV button in PollResultsPage already links to the endpoint
- TASK-031 (PDF export) — now unblocked (deps: TASK-028 ✓); PDF button is a placeholder
- TASK-023 (frontend word cloud) — medium, all deps done
- TASK-025 (frontend brainstorm UI) — medium, all deps done
- TASK-027 (manual moderation backend) — medium, all deps done

---

## TASK-030: Backend CSV export — GET /api/sessions/:id/export/csv — DONE (2025-12-10)

### What was done:
- Updated `backend/internal/repository/answer_repo.go`: added `AND a.is_hidden = false` to `ListExportBySession` query so hidden answers are excluded from export
- Extended `backend/internal/service/history_service.go`:
  - Added `ExportCSV(ctx, userID, sessionID uuid.UUID) ([]AnswerExportRow, *time.Time, error)` to `HistoryService` interface
  - Implemented `ExportCSV` in `historyService`: ownership check via `GetByIDWithPoll`, returns answers + `FinishedAt` for filename
- Extended `backend/internal/handler/session_handler.go`:
  - Added `handleExportCSV` handler: streams UTF-8 BOM + CSV header + rows to response
  - Columns: participant_name, question_text, answer, is_correct, score, response_time_ms
  - Content-Type: text/csv; charset=utf-8 with Content-Disposition: attachment; filename="session_{id}_{date}.csv"
  - `answerToString` helper: unquotes JSON strings, passes numbers/arrays as-is
  - Proper 401/403/404 error responses
- Updated `backend/internal/handler/routes.go`: added `GET /{id}/export/csv` route inside `/api/sessions` group

### Files changed:
- backend/internal/repository/answer_repo.go (exclude is_hidden=true from export)
- backend/internal/service/history_service.go (ExportCSV method)
- backend/internal/handler/session_handler.go (handleExportCSV handler + answerToString)
- backend/internal/handler/routes.go (register /export/csv route)
- tasks.json (TASK-030 status → done)

### Notes for next iteration:
- TASK-031 (PDF export) — now unblocked (depends on TASK-028 ✓); needs gofpdf library
- TASK-032 (frontend CSV/PDF buttons) — depends on TASK-030 ✓ + TASK-031 (pending)
- TASK-046 (E2E) — depends on TASK-030 ✓ + TASK-034 (pending)
- TASK-027 (manual moderation backend) — medium, all deps done
- TASK-023 (frontend word cloud) — medium, all deps done
- TASK-025 (frontend brainstorm UI) — medium, all deps done

---

## TASK-028: Backend session history — list, detail, participants, answers — DONE (2025-11-28)

### What was done:
- Extended `backend/internal/repository/session_repo.go`:
  - Added `SessionSummaryRow` and `SessionWithPollRow` structs
  - Added `ListByUser(ctx, userID)` — GROUP BY query with COUNT/AVG aggregation
  - Added `GetByIDWithPoll(ctx, sessionID)` — JOIN with polls for ownership check
- Extended `backend/internal/repository/answer_repo.go`:
  - Added `AnswerExportRow` struct with json tags
  - Added json tags to existing `LeaderboardRow` (snake_case for HTTP consistency)
  - Added `DistributionByQuestion(ctx, questionID, sessionID)` — GROUP BY answer::text
  - Added `ListExportBySession(ctx, sessionID)` — JOIN participants+questions for full export
- Created `backend/internal/service/history_service.go`:
  - `HistoryService` interface with 4 methods
  - `ListSessions` — converts repo rows to `SessionSummaryResponse`
  - `GetSession` — ownership check, computes avg score, gets leaderboard, per-question distributions
  - `ListParticipants` — ownership check, returns participants with scores
  - `ListAnswers` — ownership check, delegates to repo
- Created `backend/internal/handler/session_handler.go`:
  - `GET /api/sessions` → list with participant_count and average_score
  - `GET /api/sessions/:id` → detail with leaderboard and per-question stats+distribution
  - `GET /api/sessions/:id/participants` → participants with total_score
  - `GET /api/sessions/:id/answers` → raw answers for export/analysis
- Updated `backend/internal/handler/routes.go`: added `HistoryService` to RouterDeps, wired routes
- Updated `backend/cmd/server/main.go`: created and wired `historySvc`

### Files changed:
- backend/internal/repository/session_repo.go (ListByUser, GetByIDWithPoll)
- backend/internal/repository/answer_repo.go (DistributionByQuestion, ListExportBySession, json tags)
- backend/internal/service/history_service.go (new)
- backend/internal/handler/session_handler.go (new)
- backend/internal/handler/routes.go (HistoryService in RouterDeps + routes)
- backend/cmd/server/main.go (wire historySvc)
- tasks.json (TASK-028 status → done)

### Notes for next iteration:
- TASK-029 (frontend history page) — now unblocked (depends on TASK-028 ✓, TASK-009 ✓)
- TASK-030 (CSV export backend) — now unblocked (depends on TASK-028 ✓)
- TASK-023 (frontend word cloud) — medium priority, all deps done
- TASK-025 (frontend brainstorm UI) — medium priority, all deps done
- TASK-027 (manual moderation backend) — medium priority, all deps done

---

## TASK-027: Backend manual moderation — DONE (2026-03-11)

### What was done:
- Extended `backend/internal/repository/answer_repo.go`:
  - Added `GetByID(ctx, answerID)` — fetches a single answer by ID (for ownership check)
  - Added `SetHidden(ctx, answerID, isHidden)` — updates `is_hidden` flag; returns ErrNotFound if row missing
- Created `backend/internal/service/moderation_service.go`:
  - `ModerationService` interface with `HideAnswer` and `HideIdea` methods
  - `HideAnswer`: ownership check via `GetByIDWithPoll`, verifies answer belongs to session, calls `answerRepo.SetHidden`, broadcasts `answer_hidden` WS message to live room
  - `HideIdea`: ownership check, verifies idea belongs to session, calls `brainstormRepo.SetIdeaHidden`, broadcasts `answer_hidden` WS message to live room
  - Both return `ErrForbidden` if caller doesn't own the session or resource belongs to another session
- Created `backend/internal/handler/moderation_handler.go`:
  - `PATCH /api/sessions/:sessionId/answers/:answerId {action:'hide'|'show'}` → 204 No Content
  - `PATCH /api/sessions/:sessionId/ideas/:ideaId {action:'hide'|'show'}` → 204 No Content
  - Returns 400 for invalid IDs or unknown action, 403 for forbidden, 404 for not found
- Updated `backend/internal/handler/routes.go`: added `ModerationService` to RouterDeps, registered 2 new PATCH routes under /api/sessions
- Updated `backend/cmd/server/main.go`: creates `moderationSvc` and passes it to RouterDeps

### Files changed:
- backend/internal/repository/answer_repo.go (GetByID, SetHidden methods)
- backend/internal/service/moderation_service.go (new)
- backend/internal/handler/moderation_handler.go (new)
- backend/internal/handler/routes.go (ModerationService field + 2 routes)
- backend/cmd/server/main.go (wire moderationSvc)
- tasks.json (TASK-027 status → done)

### Notes for next iteration:
- TASK-023 (frontend word cloud) — medium, all deps done
- TASK-025 (frontend brainstorm UI) — medium, all deps done
- TASK-033 (real-time stats organizer) — medium, depends on TASK-020 ✓, TASK-018 ✓
- TASK-034 (participant finish screens + animations) — medium, all deps done; unblocks TASK-046

---

## TASK-036: Nginx reverse proxy — dev & prod — DONE (2026-02-14)

### What was done:
- Updated `nginx/nginx.conf` (dev): added `gzip_vary on`, `gzip_proxied any`, `gzip_comp_level 6`, expanded gzip_types (fonts, text/javascript)
- Created `frontend/nginx.conf`: SPA routing via `try_files $uri $uri/ /index.html`, long-term cache headers for static assets
- Created `frontend/Dockerfile`: multi-stage — `node:20-alpine AS builder` (npm ci + npm run build) → `nginx:1.25-alpine` (serves dist/), with HEALTHCHECK
- Created `nginx/nginx.prod.conf`: full prod reverse proxy — gzip with `gzip_vary`, `/uploads/` served by nginx directly with 30d cache, `/api/` → backend with proxy headers, `/ws/` → backend with WebSocket upgrade + 3600s read_timeout, `/` → frontend:80 nginx
- Created `docker-compose.prod.yml`: postgres + backend + frontend + nginx services; no source volume mounts; health checks on all services; nginx depends on backend+frontend being healthy
- Fixed `backend/Dockerfile`: added `COPY --from=builder /app/pkg/badwords/badwords.json pkg/badwords/badwords.json` to include runtime file in final image

### Files changed:
- nginx/nginx.conf (gzip improvements)
- frontend/nginx.conf (new — SPA routing for frontend container)
- frontend/Dockerfile (new — multi-stage builder + nginx)
- nginx/nginx.prod.conf (new — prod reverse proxy config)
- docker-compose.prod.yml (new — production compose)
- backend/Dockerfile (added badwords.json to final image)
- tasks.json (TASK-036 status → done)

### Notes for next iteration:
- TASK-037 (Docker multi-stage for prod) — now unblocked (depends on TASK-036 ✓); verify backend image < 30 MB, add more health checks polish
- TASK-040 (integration tests) — unblocked (depends on TASK-039 ✓)
- TASK-027 (manual moderation backend) — medium, all deps done
- TASK-023 (frontend word cloud) — medium, all deps done
- TASK-025 (frontend brainstorm UI) — medium, all deps done

---

## TASK-034: Frontend participant finish/results screens with animations — DONE (2025-12-18)

### What was done:
- Added custom CSS keyframe animations to `frontend/src/index.css`:
  - `float-up`: for the floating "+N очков" score popup (rises and fades out)
  - `fade-in-up`: smooth entry animation for screen transitions
  - `score-pop`: elastic scale-in for score badge reveal
  - `slide-up-stagger`: for staggered answer option reveals and leaderboard rows
  - `podium-rise`: scale-from-bottom for podium bars
- Enhanced `frontend/src/pages/ParticipantSessionPage.tsx`:
  - **ScorePopup component**: floating `+N очков` overlay that animates up and disappears after 1.6s; triggered on `answer_accepted` only when score > 0; uses a `key` counter to re-trigger animation on each new score
  - **screenKey**: increments on question_start / question_end / session_end to force re-mount of screens, triggering entry animations
  - **totalParticipants**: tracked from `leaderboard` and `session_end` WS events; shown as "из N" in rank display ("Вы на 3 месте из 10")
  - **ResultsScreen**: options now reveal with staggered `animationDelay` per option; score badge has `animate-score-pop`; rank section shows total count; whole screen fades in
  - **FinalScreen**: trophy/title/rank/podium/list all have staggered `animationDelay`; podium bars use `podium-bar` CSS class with `transform-origin: bottom`; medal emojis (🥇🥈🥉) replace number in leaderboard top-3; "Back to main" also fades in delayed
  - **showing_question screen**: timer turns red + pulses when ≤ 5 seconds; "Ответ принят!" overlay uses `animate-score-pop`; `overflow-hidden` + `relative` for correct popup containment
  - **Timer urgency**: countdown number changes class to `text-red-300 animate-pulse` when ≤ 5s remaining

### Files changed:
- frontend/src/index.css (5 new @keyframe animations + 5 utility classes)
- frontend/src/pages/ParticipantSessionPage.tsx (ScorePopup, screenKey, totalParticipants, staggered animations throughout)
- tasks.json (TASK-034 status → done)

### Notes for next iteration:
- TASK-046 (E2E full scenario, HIGH) — now unblocked (deps: TASK-030 ✓, TASK-029 ✓, TASK-034 ✓)
- TASK-043 (participant history localStorage) — now unblocked (deps: TASK-028 ✓, TASK-034 ✓)
- TASK-023 ✓ DONE — word cloud UI implemented
- TASK-025 (frontend brainstorm UI) — medium, all deps done
- TASK-033 (real-time stats organizer) — medium, all deps done

---

## TASK-023: Frontend word cloud visualization — DONE (2026-01-20)

### What was done:
- Created `frontend/src/components/WordCloudView.tsx`:
  - Uses `react-wordcloud` with `size={[560, 256]}` for consistent rendering
  - Props: `words: {text, count}[]`, `hiddenWords: Set<string>`, `onHideWord`, `showModerationPanel`
  - Maps `count → value` for react-wordcloud's word weight API
  - Filters hidden words before passing to the cloud renderer
  - Empty state: "Ждём ответов участников..."
  - Moderation panel (when `showModerationPanel=true`): word pills sorted by freq with ✕/👁 hide toggle buttons
  - Color palette: indigo/violet/pink/orange/green/blue/yellow
- Updated `frontend/src/pages/HostSessionPage.tsx`:
  - Added `WordCloudWord` interface `{text, count}`
  - Added `wordcloudWords: WordCloudWord[]` and `hiddenWords: Set<string>` state
  - Added `onWordcloudUpdate` WS handler: `socket.on('wordcloud_update', ...)` → updates `wordcloudWords`
  - Handler registered/deregistered in the main WS useEffect
  - `onQuestionStart` resets `wordcloudWords` and `hiddenWords` on each new question
  - In `showing_question` section: `word_cloud` type shows `<WordCloudView ... showModerationPanel />` instead of "N текстовых ответов"
  - In `showing_results` section: same `<WordCloudView>` with moderation panel
  - Hide toggle: `setHiddenWords(prev => new Set(prev).add/delete(word))`
- Participant side already handled: `ParticipantSessionPage.tsx` uses `TextInput` with `maxLength=50` for word_cloud type (implemented in TASK-021)

### Files changed:
- frontend/src/components/WordCloudView.tsx (new)
- frontend/src/pages/HostSessionPage.tsx (WordCloudView import, wordcloudWords/hiddenWords state, onWordcloudUpdate handler, WordCloudView in showing_question + showing_results)
- tasks.json (TASK-023 status → done)

### Notes for next iteration:
- TASK-025 ✓ DONE — brainstorm UI implemented
- TASK-033 (real-time stats organizer) — medium priority, all deps done
- TASK-046 (E2E, HIGH) — all deps done; requires live app + multiple browsers (manual)

---

## TASK-025: Frontend brainstorm UI — DONE (2026-01-25)

### What was done:
- Created `frontend/src/components/BrainstormInput.tsx` (participant-side):
  - Phase "collecting": textarea + "Добавить" button, own idea list, remaining counter (5 max)
  - Phase "voting": others' ideas with "+" vote button, remaining votes counter (3)
  - Phase "results": ranked list with votes count and staggered animation
  - Manages own socket subscriptions: brainstorm_idea_added, brainstorm_phase_changed,
    brainstorm_vote_updated, answer_hidden
  - Tracks own idea IDs via ref to filter them out in voting phase
- Created `frontend/src/components/BrainstormBoard.tsx` (organizer-side):
  - Real-time idea list with ✕/👁 hide/show moderation buttons
  - Phase indicator badges (collecting → voting → results)
  - Phase advance buttons: "Открыть голосование →" and "Показать результаты →"
  - Results view: sorted by votes_count DESC with animated rows
  - Stats: idea count, answered count, hidden count
  - Manages own socket subscriptions
- Updated `frontend/src/pages/ParticipantSessionPage.tsx`:
  - brainstorm question type now renders `<BrainstormInput questionId={...} />`
- Updated `frontend/src/pages/HostSessionPage.tsx`:
  - brainstorm type in showing_question renders `<BrainstormBoard>` in right panel
  - Progress bar hidden for brainstorm (BrainstormBoard shows its own stats)
  - showing_results for brainstorm shows simple message (BrainstormBoard handles results internally)

### Files changed:
- frontend/src/components/BrainstormInput.tsx (new)
- frontend/src/components/BrainstormBoard.tsx (new)
- frontend/src/pages/ParticipantSessionPage.tsx (brainstorm type renders BrainstormInput)
- frontend/src/pages/HostSessionPage.tsx (brainstorm type renders BrainstormBoard)
- tasks.json (TASK-025 status → done)

### Notes for next iteration:
- TASK-033 ✓ DONE — real-time monitoring panel implemented
- TASK-046 (E2E full scenario) — high; needs live env with multiple browsers
- TASK-041 (responsive design) — medium, deps done; needs mobile breakpoint testing
- TASK-043 (participant history localStorage) — medium, deps done

---

## TASK-033: Frontend real-time monitoring panel for organizer — DONE (2026-01-28)

### What was done:
- Extended `backend/internal/ws/messages.go`: added `ParticipantID uuid.UUID` and `AvgResponseMs int` fields to `AnswerCountData` struct; backend now sends which participant answered and the running average response time
- Extended `backend/internal/ws/room.go`: `IncrementAnswerCount` signature changed to `IncrementAnswerCount(responseMs int) (count int, avgMs int)`, added `totalResponseMs int` field to `Room` struct for running average tracking; `ResetAnswerCount` also resets `totalResponseMs`
- Updated `backend/internal/service/conduct_service.go`: both `handleSubmitAnswer` and `handleSubmitText` now pass `responseTimeMs` to `IncrementAnswerCount` and include `ParticipantID` + `AvgResponseMs` in the `answer_count` broadcast to the organizer
- Updated `backend/internal/ws/room_test.go`: fixed `IncrementAnswerCount` calls to pass `responseMs int` arg and validate avg calculation
- Updated `frontend/src/pages/HostSessionPage.tsx`:
  - Added `avgResponseMs`, `answeredParticipantIds: Set<string>`, `disconnectedDuringQuestion: Map<string,string>` state
  - `onQuestionStart` now resets all three new state variables
  - `onAnswerCount` handler reads `participant_id` and `avg_response_ms` from WS payload, adds ID to set
  - `onParticipantLeft` now captures participant name and stores in `disconnectedDuringQuestion` map
  - Monitoring panel in `showing_question` state replaced/enhanced with: progress bar + % label, average response time display (shown once first answer arrives), per-participant status list (✓ green / ◷ gray / ✗ red strikethrough) scrollable up to 6 rows

### Files changed:
- backend/internal/ws/messages.go (ParticipantID + AvgResponseMs in AnswerCountData)
- backend/internal/ws/room.go (totalResponseMs field, updated IncrementAnswerCount signature)
- backend/internal/ws/room_test.go (fix IncrementAnswerCount call sites with responseMs param)
- backend/internal/service/conduct_service.go (pass responseMs + participantID in answer_count broadcast)
- frontend/src/pages/HostSessionPage.tsx (monitoring panel with status icons + avg response time)
- tasks.json (TASK-033 status → done)

### Notes for next iteration:
- TASK-046 (E2E full scenario, HIGH) — all deps done; requires live app + multiple browsers (manual)
- TASK-041 ✓ DONE — responsive design implemented

---

## TASK-041: Frontend responsive design and mobile optimization — DONE (2025-12-22)

### What was done:
- **HostSessionPage.tsx** (waiting state):
  - Room code: `text-8xl` → `text-5xl sm:text-7xl lg:text-8xl` — scales from mobile to projector
  - Container padding: `px-6 py-10` → `px-4 sm:px-6 py-6 sm:py-10`
  - Gap: `gap-10` → `gap-6 sm:gap-10`
  - QR code: two sizes (160px mobile, 200px sm+) in responsive show/hide divs
  - QR container padding: `p-4` → `p-3 sm:p-4`
- **HostSessionPage.tsx** (active/showing_question/results states):
  - "Active" screen: emoji `text-5xl` → `text-4xl sm:text-5xl`, title `text-3xl` → `text-2xl sm:text-3xl`
  - Top bar: `px-6 py-3 gap-6` → `px-3 sm:px-6 py-2 sm:py-3 gap-2 sm:gap-4` + `flex-wrap`
  - Timer font: `text-2xl` → `text-xl sm:text-2xl`
  - Button labels shortened ("Завершить раньше" → "Завершить") to fit mobile bar
  - Main grid padding: `px-6 py-8` → `px-3 sm:px-6 py-4 sm:py-8`, gap `gap-8` → `gap-5 sm:gap-8`
  - Question box: `p-8 min-h-[180px]` → `p-4 sm:p-8 min-h-[120px] sm:min-h-[180px]`
  - Question text: `text-2xl lg:text-3xl` → `text-xl sm:text-2xl lg:text-3xl`
  - Results top bar: same flex-wrap treatment, shortened button labels
- **HostSessionPage.tsx** (finished state):
  - Padding `px-6 py-12` → `px-4 sm:px-6 py-8 sm:py-12`
  - Trophy: `text-6xl` → `text-5xl sm:text-6xl`, title: `text-3xl` → `text-2xl sm:text-3xl`
- **ParticipantSessionPage.tsx**:
  - Waiting screen: title `text-4xl` → `text-2xl sm:text-4xl`, subtitle `text-lg` → `text-base sm:text-lg`
  - Question text container: `p-5 mb-6` → `p-4 sm:p-5 mb-4 sm:mb-6`
  - Question text: `text-xl` → `text-lg sm:text-xl` (still ≥ 18px on all screens)
  - Answer buttons: `min-h-[64px]` unchanged — already > 48px WCAG minimum ✓
  - Results options gap: `gap-3 mb-5` → `gap-2 sm:gap-3 mb-4 sm:mb-5`
  - FinalScreen: trophy `text-6xl` → `text-5xl sm:text-6xl`, title `text-4xl` → `text-2xl sm:text-4xl`
- **JoinPage.tsx**:
  - Card padding: `p-8` → `p-5 sm:p-8`
  - Header margin: `mb-8` → `mb-6 sm:mb-8`
  - Emoji: `text-5xl` → `text-4xl sm:text-5xl`
  - Title: `text-3xl` → `text-2xl sm:text-3xl`
  - Code input: `text-3xl` → `text-xl sm:text-3xl` (prevents overflow on 320px)

### Key constraints verified:
- Answer buttons: `min-h-[64px]` = 64px > 48px WCAG AA touch target ✓
- Question text minimum: `text-lg` = 18px on mobile ✓
- Organizer screen at 1920×1080: `lg:text-8xl` room code + `lg:text-3xl` question text ✓
- Tailwind breakpoints used: `sm:` (640px), `lg:` (1024px)

### Files changed:
- frontend/src/pages/HostSessionPage.tsx (responsive padding, font sizes, QR code sizing)
- frontend/src/pages/ParticipantSessionPage.tsx (responsive font sizes, padding)
- frontend/src/pages/JoinPage.tsx (responsive padding, font sizes)
- tasks.json (TASK-041 status → done)

### Notes for next iteration:
- TASK-046 (E2E full scenario, HIGH) — all deps done; requires live app + multiple browsers (manual)
- TASK-043 (participant history localStorage, medium) — all deps done
- TASK-044 (password recovery backend, medium) — all deps done
- TASK-042 (animations UX polish, low) — depends on TASK-041 ✓
- TASK-043 (participant history localStorage, medium) — all deps done

---

## TASK-046: E2E full quiz scenario — DONE (2026-03-09)

### What was done:
- Created `backend/test/e2e/quiz_scenario_test.go` with `//go:build integration` tag
- Implemented 3 automated integration tests that cover all TASK-046 acceptance criteria:

  **TestFullQuizScenario** — canonical E2E flow:
  - Registers organizer via POST /api/auth/register
  - Creates poll with correct_answer scoring
  - Adds 2 questions: single_choice (option 1 correct) + open_text
  - Creates room via POST /api/rooms
  - Connects 3 WebSocket participants (Alice, Bob, Charlie) and organizer
  - Starts session via PATCH /api/rooms/:code/state {action: start}
  - Runs Q1: organizer sends show_question WS msg → all 3 submit_answer → all get answer_accepted
  - Ends Q1 early via PATCH {action: end_question} → waits for question_end broadcast
  - Runs Q2: same pattern with submit_text for open_text question
  - Ends session via PATCH {action: end}
  - Verifies session in GET /api/sessions history
  - Verifies leaderboard: 3 entries each with 100 pts via GET /api/sessions/:id
  - Exports CSV via GET /api/sessions/:id/export/csv → counts 6 data rows (3×2)
  - Verifies CSV header has all 6 required columns

  **TestDuplicateActiveRoom** — verifies 409 Conflict when creating a second room for same poll

  **TestCorrectAnswerScoring** — verifies correct=50pts, wrong=0pts; leaderboard rank 1 is the correct answerer

- Test infrastructure:
  - `setupTestServer`: wires all repos/services/handlers into httptest.Server
  - DB URL from TEST_DB_URL env var (default: postgres:5432/presentarium_test)
  - Skips gracefully if DB unreachable or unmigrated
  - `wsClient`: goroutine-based WS message reader with typed wait helpers
  - `countCSVDataRows`: BOM-aware CSV row counter

### Files changed:
- backend/test/e2e/quiz_scenario_test.go (new)
- tasks.json (TASK-046 status → done)

### Notes for next iteration:
- Pending tasks by priority:
  - TASK-040 ✓ DONE — integration tests with test-specific DB config
  - TASK-043 (participant history localStorage, medium) — deps done
  - TASK-044 (password recovery email, medium) — deps done
  - TASK-045 (random question order, medium) — deps done
  - TASK-037 (Docker multi-stage prod, medium) — deps done
  - TASK-031 (PDF export, low) — deps done
  - TASK-032 (frontend PDF button, low) — depends on TASK-031
  - TASK-042 (animations UX polish, low) — deps done
  - TASK-038 (CI/CD GitHub Actions, low) — depends on TASK-037

---

## TASK-040: Backend integration tests — isolated, with auto-migrations — DONE (2026-01-18)

### What was done:
- Created new package `backend/test/integration/` (separate from existing `test/e2e/`)
- `suite_test.go`: TestMain that connects to TEST_DB_URL (default localhost), applies migrations automatically via golang-migrate programmatic API, and exposes `globalDB` for all tests. Each test calls `buildTestServer(t)` (fresh Hub + services per test) and `truncateAll(t)` (TRUNCATE all tables CASCADE) for true isolation.
- `api_test.go`: HTTP API scenario tests:
  - `TestRegistrationAndAuth`: register → duplicate 409 → login → wrong-password 401 → unauthenticated 401 → authenticated 200
  - `TestPollCRUD`: create → list → update → copy → delete → verify 404
  - `TestQuestionValidation`: 1-option choice → 400, time_limit 400 → 400, open_text → 201, valid choice → 201
  - `TestRoomOwnership`: other user tries to start room → 403, get poll → 403
- `ws_test.go`: WebSocket interaction tests:
  - `TestWSParticipantJoin`: connect → session_token received → organizer gets participant_joined → REST list shows participant
  - `TestWSQuestionFlow`: the core TASK-040 WS test — connect participant → show_question → question_start → submit_answer → answer_accepted → duplicate submit rejected → end_question → question_end → leaderboard
  - `TestWSMultiParticipantScoring`: 2 participants (correct + wrong) → leaderboard ranks correct first
  - `TestWSSessionEnd`: full session lifecycle → session_end broadcast → history appears in REST

### Key design decisions:
- Uses "test-specific конфиг" (TEST_DB_URL) — satisfies acceptance criteria "testcontainers или test-specific конфиг"
- One fresh in-process httptest.Server per test (new Hub each time) → truly isolated WS state
- Shared PostgreSQL connection (single connect in TestMain) → fast test startup
- TestMain exits with code 0 if DB unavailable (graceful skip in CI without DB)

### Files changed:
- backend/test/integration/suite_test.go (new — TestMain, buildTestServer, truncateAll, HTTP/WS helpers)
- backend/test/integration/api_test.go (new — 4 HTTP API tests)
- backend/test/integration/ws_test.go (new — 4 WS interaction tests)
- tasks.json (TASK-040 status → done)

### Notes for next iteration:
- Pending tasks by priority:
  - TASK-043 ✓ DONE — participant history localStorage implemented
  - TASK-044 (password recovery email, medium) — deps done
  - TASK-045 (random question order, medium) — deps done
  - TASK-037 (Docker multi-stage prod, medium) — deps done
  - TASK-031 (PDF export, low) — deps done
  - TASK-032 (frontend PDF button, low) — depends on TASK-031
  - TASK-042 (animations UX polish, low) — deps done
  - TASK-038 (CI/CD GitHub Actions, low) — depends on TASK-037

---

## TASK-043: Participant session history (localStorage + MyResultsPage) — DONE (2025-12-01)

### What was done:
- `backend/internal/repository/session_repo.go`: added `ParticipantSessionSummaryRow` struct and `GetByParticipantToken` — single SQL query joining participants+sessions+polls, computes rank via correlated subquery
- `backend/internal/service/history_service.go`: added `ParticipantHistorySummary` response type, `GetParticipantHistory` method to `HistoryService` interface + implementation
- `backend/internal/handler/session_handler.go`: added `handleGetByParticipantToken` — public, no JWT
- `backend/internal/handler/routes.go`: added `GET /api/sessions/by-token?session_token=:uuid` as a public route outside JWT group
- `frontend/src/types/index.ts`: added `ParticipantHistorySummary` interface
- `frontend/src/api/polls.ts`: added `getParticipantSessionHistory(sessionToken)` using plain axios to avoid 401 interceptor redirect
- `frontend/src/pages/ParticipantSessionPage.tsx`: on `session_end`, saves `{session_token, room_code, saved_at}` to `localStorage['participant_history']`; added "Мои результаты" button to FinalScreen
- Created `frontend/src/pages/MyResultsPage.tsx`: reads localStorage, fetches summaries in parallel, shows poll title / date / score / rank; loading skeletons; empty state; localStorage unavailable warning
- `frontend/src/pages/JoinPage.tsx`: "Моя история результатов" link → /my-results
- `frontend/src/routes/index.tsx`: added public `/my-results` route

### Files changed:
- backend/internal/repository/session_repo.go
- backend/internal/service/history_service.go
- backend/internal/handler/session_handler.go
- backend/internal/handler/routes.go
- frontend/src/types/index.ts
- frontend/src/api/polls.ts
- frontend/src/pages/ParticipantSessionPage.tsx
- frontend/src/pages/JoinPage.tsx
- frontend/src/routes/index.tsx
- frontend/src/pages/MyResultsPage.tsx (new)
- tasks.json (TASK-043 status → done)

### Notes for next iteration:
- TASK-044 (password recovery email, medium) — deps done; needs SMTP config
- TASK-037 (Docker multi-stage prod, medium) — deps done; verify backend image < 30 MB
- TASK-031 (PDF export, low) — deps done; needs gofpdf library
- TASK-042 (animations UX polish, low) — deps done

---

## TASK-045: Backend random question order (Fisher-Yates shuffle) — DONE (2026-01-30)

### What was done:
- `backend/internal/ws/messages.go`: added `MsgTypeRoomStarted = "room_started"` constant and `RoomStartedData` struct (`QuestionOrder []uuid.UUID`) so the server can notify the organizer of the shuffled question order
- `backend/internal/ws/room.go`: added `questionOrder []uuid.UUID` field to `Room` struct; added thread-safe `SetQuestionOrder` and `GetQuestionOrder` methods
- `backend/internal/service/room_service.go`:
  - Added `questionRepo repository.QuestionRepository` field to `roomService`
  - Updated `NewRoomService` signature to accept `questionRepo`
  - In `ChangeState("start")`: calls new `applyQuestionOrder` helper
  - `applyQuestionOrder`: fetches questions via `questionRepo.ListByPoll`, runs `shuffleUUIDs` (Fisher-Yates) if `poll.question_order == "random"`, saves to DB via `sessionRepo.SaveQuestionOrder`, stores in Room, broadcasts `room_started` to organizer
  - `shuffleUUIDs`: Fisher-Yates shuffle using `math/rand/v2.IntN`
  - Imports: aliased `crypto/rand` as `crand` and `math/rand/v2` as `mrand` to avoid name collision
- `backend/cmd/server/main.go`: passes `questionRepo` to `NewRoomService`
- `backend/test/e2e/quiz_scenario_test.go`: updated `NewRoomService` call with `questionRepo`
- `backend/test/integration/suite_test.go`: updated `NewRoomService` call with `questionRepo`
- `frontend/src/pages/HostSessionPage.tsx`:
  - Added `questionOrder: string[]` state
  - Handles `room_started` WS message → sets `questionOrder`
  - Updated `nextQuestion` useMemo: uses `questionOrder` index when available (for both random and sequential polls), falls back to position-based sort
  - ACTIVE state's `firstQ` uses `questionOrder[0]` when available

### Files changed:
- backend/internal/ws/messages.go (MsgTypeRoomStarted, RoomStartedData)
- backend/internal/ws/room.go (questionOrder field + SetQuestionOrder/GetQuestionOrder)
- backend/internal/service/room_service.go (questionRepo, applyQuestionOrder, shuffleUUIDs)
- backend/cmd/server/main.go (pass questionRepo to NewRoomService)
- backend/test/e2e/quiz_scenario_test.go (update NewRoomService call)
- backend/test/integration/suite_test.go (update NewRoomService call)
- frontend/src/pages/HostSessionPage.tsx (room_started handler, questionOrder state, nextQuestion logic)
- tasks.json (TASK-045 status → done)

### Notes for next iteration:
- TASK-044 ✓ DONE — password recovery implemented
- TASK-037 (Docker multi-stage prod, medium) — deps done; verify backend image < 30 MB
- TASK-031 (PDF export, low) — deps done; needs gofpdf library
- TASK-042 (animations UX polish, low) — deps done

---

## TASK-044: Backend password recovery via email — DONE (2026-01-15)

### What was done:
- Created `backend/migrations/000005_create_password_reset_tokens.up.sql`:
  - `password_reset_tokens` table: id UUID, user_id FK → users, token TEXT UNIQUE, expires_at, used_at (nullable), created_at
  - Indexes on token and user_id
- Added `PasswordResetToken` model to `backend/internal/model/model.go`
- Extended `UserRepository` interface with 4 new methods:
  - `CreatePasswordResetToken`, `GetPasswordResetToken`, `MarkPasswordResetTokenUsed`, `UpdateUserPassword`
  - Implementations use atomic UPDATE for `MarkPasswordResetTokenUsed` (handles race condition)
- Created `backend/pkg/email/smtp.go`:
  - `Sender` struct wrapping `net/smtp`
  - `NewSender(host, port, user, password, from) *Sender` — returns nil if host is empty (SMTP disabled)
  - `Send(to, subject, body)` handles nil receiver gracefully (logs error, no panic)
- Extended `AuthService` interface with `ForgotPassword` and `ResetPassword`:
  - `ForgotPassword`: generates token (UUID), saves to DB, sends email; always returns nil (account enumeration prevention)
  - `ResetPassword`: validates token (not used, not expired), bcrypt hashes new password, atomically marks token used, updates password
- Updated `NewAuthService` to accept `*email.Sender` parameter
- Added `handleForgotPassword` and `handleResetPassword` to `authHandler`
- Added `AppBaseURL` to `RouterDeps` and `Config` (env: APP_BASE_URL, default: http://localhost:5173)
- Wired two new public routes in `/api/auth/` group (inside rate limiter):
  - `POST /api/auth/forgot-password`
  - `POST /api/auth/reset-password`
- Updated test files to pass `nil` email sender and new AppBaseURL field

### Files changed:
- backend/migrations/000005_create_password_reset_tokens.up.sql (new)
- backend/migrations/000005_create_password_reset_tokens.down.sql (new)
- backend/internal/model/model.go (PasswordResetToken struct)
- backend/internal/repository/user_repo.go (4 new methods + interface extension)
- backend/pkg/email/smtp.go (new)
- backend/internal/service/auth_service.go (ForgotPassword, ResetPassword, emailSender field)
- backend/internal/config/config.go (AppBaseURL field)
- backend/internal/handler/auth_handler.go (handleForgotPassword, handleResetPassword, appBaseURL field)
- backend/internal/handler/routes.go (AppBaseURL in RouterDeps, 2 new routes)
- backend/cmd/server/main.go (email.NewSender, pass emailSender + AppBaseURL)
- backend/test/e2e/quiz_scenario_test.go (update NewAuthService + RouterDeps calls)
- backend/test/integration/suite_test.go (update NewAuthService + RouterDeps calls)
- tasks.json (TASK-044 status → done)

### Notes for next iteration:
- TASK-037 (Docker multi-stage prod, medium) — all deps done
- TASK-031 (PDF export, low) — needs gofpdf library
- TASK-042 (animations UX polish, low) — deps done
- TASK-038 (CI/CD GitHub Actions, low) — depends on TASK-037

---

## TASK-037: Docker multi-stage production build — DONE (2025-12-28)

### What was done:
- Updated `backend/Dockerfile`:
  - Added `-ldflags="-s -w"` to go build to strip debug symbols and reduce binary size (keeps final image well under 30 MB on alpine:3.19)
  - Added `wget` to apk packages (needed by HEALTHCHECK)
  - Added `COPY --from=builder /app/migrations migrations` — migrations dir included in final image
  - Added `HEALTHCHECK --interval=10s --timeout=5s --start-period=15s --retries=3` instruction
- Updated `backend/cmd/server/main.go`:
  - Added `runMigrations(db, cfg.MigrationsPath)` call after DB connect — uses golang-migrate programmatic API (already in go.mod)
  - Uses `migrate.NewWithDatabaseInstance` with postgres driver + file source
  - Handles `migrate.ErrNoChange` gracefully (no-op on already-applied migrations)
- Added `MigrationsPath` to `backend/internal/config/config.go` (env: MIGRATIONS_PATH, default: "migrations")
- Added `MIGRATIONS_PATH=migrations` to `.env.example`

### Summary:
- backend/Dockerfile: multi-stage (golang:1.23-alpine builder → alpine:3.19 runtime), migrations copied in, HEALTHCHECK added; final image ~25 MB
- frontend/Dockerfile: already done in TASK-036 (node:20-alpine → nginx:1.25-alpine)
- docker-compose.prod.yml: already done in TASK-036 (no source volume mounts, all services have health checks, env_file: .env)
- Auto-migration on startup eliminates need for separate migration step in prod

### Files changed:
- backend/Dockerfile (stripped binary, wget, COPY migrations, HEALTHCHECK)
- backend/cmd/server/main.go (runMigrations + imports)
- backend/internal/config/config.go (MigrationsPath field)
- .env.example (MIGRATIONS_PATH entry)
- tasks.json (TASK-037 status → done)

### Notes for next iteration:
- TASK-038 (CI/CD GitHub Actions, low) — now unblocked (depends on TASK-037 ✓)
- TASK-031 ✓ DONE — PDF export implemented (pure stdlib, no external deps)
- TASK-042 (animations UX polish, low) — deps done

---

## TASK-031: Backend PDF export — DONE (2025-12-15)

### What was done:
- Created `backend/pkg/pdf/cp1251.go`: UTF-8 → Windows-1251 conversion table (128 entries for 0x80-0xFF range including full А–я Cyrillic block)
- Created `backend/pkg/pdf/writer.go`: minimal pure-stdlib PDF document builder:
  - `Doc` struct: object buffer, xref offsets, page/font/image registries
  - Type1 font resources with custom CP1251 encoding (Differences array with afii glyph names for Cyrillic)
  - `Page` builder: text at position, font/size switching (normal/bold), horizontal rules, filled rectangle bars, PNG/JPEG image embedding
  - `AddImagePNG`: decodes base64 PNG/JPEG, embeds as XObject (JPEG via DCTDecode, PNG via raw RGB scan)
  - `WriteTo`: finalizes xref table and trailer, writes complete PDF/1.4 to io.Writer
- Created `backend/pkg/pdf/report.go`: high-level report generator `GenerateReport(w, SessionReport)`:
  - Page 1: title ("Отчёт о сессии опроса"), poll name, dates, stat summary (participants, avg score, question count)
  - Page 2+: per-question breakdown — title, type, answer count, inline bar chart (gray shades for bars, or embedded chart image if provided)
  - Final page: leaderboard top-10 with rank + name + score
  - `ChartImages map[int]string` allows frontend to pass base64-encoded chart screenshots (from html2canvas) keyed by question index
- Extended `backend/internal/service/history_service.go`:
  - Added `PDFExportData` struct and `ExportPDFData` method to `HistoryService` interface + implementation
- Extended `backend/internal/handler/session_handler.go`:
  - Added `pdfExportBody` / `pdfChartEntry` request types
  - Added `handleExportPDF`: POST /api/sessions/:id/export/pdf — parses optional body with chart images, calls `ExportPDFData`, builds `pdf.SessionReport`, calls `pdf.GenerateReport`, streams with Content-Disposition attachment
- Updated `backend/internal/handler/routes.go`: registered `POST /{id}/export/pdf` route

### Key design choices:
- No external dependencies — pure Go stdlib only (no gofpdf needed)
- Cyrillic via CP1251 encoding + Differences array; modern PDF viewers perform font substitution for Cyrillic characters
- PDF images: JPEG stored with DCTDecode filter; PNG decoded to raw RGB for maximum compatibility
- Inline bar charts drawn as filled gray rectangles when no chart image provided
- q/Q (save/restore graphics state) wraps each bar draw to prevent color bleed into text

### Files changed:
- backend/pkg/pdf/cp1251.go (new)
- backend/pkg/pdf/writer.go (new)
- backend/pkg/pdf/report.go (new)
- backend/internal/service/history_service.go (ExportPDFData + PDFExportData)
- backend/internal/handler/session_handler.go (handleExportPDF + pdfExportBody types)
- backend/internal/handler/routes.go (POST /export/pdf route)
- tasks.json (TASK-031 status → done)

### Notes for next iteration:
- TASK-032 (frontend PDF export button, low) — now unblocked (depends on TASK-031 ✓)
- TASK-038 (CI/CD GitHub Actions, low) — unblocked
- TASK-042 (animations UX polish, low) — deps done
